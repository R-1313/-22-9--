#include <iostream>
using namespace std;

const int N = 10;

int main() {
    int x, s = 0, m = 1;
    for (int i = 0; i < N; i++) {
        cin >> x;
        if (x > 0) s += x;
        if (x < 0) m *= x;
    }
    cout << "summ = " << s << endl;
    cout << "mult = " << m << endl;
    return 0;
}
